# Legacy placeholder

Pi camera acquisition belongs to the isolated monitoring service; the Windows GUI will
consume monitoring APIs without accessing Pi camera hardware directly. This directory
contains no active implementation and is retained to avoid deleting user files.

