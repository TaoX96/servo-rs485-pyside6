# Legacy placeholder

The former generic Raspberry Pi boundary has been replaced by the separate
`knee_rig.motion` and `knee_rig.monitoring` responsibilities. This directory contains
no active implementation and is retained to avoid deleting user files.

