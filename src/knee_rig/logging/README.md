# Legacy placeholder

This directory predates the distributed architecture and contains no implementation.
Future logging code must preserve motion/monitoring failure isolation and may use shared
contracts from `knee_rig.common`. It is retained to avoid deleting user files.

